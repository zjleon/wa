_waq.push(['commandType/Type', 'command1/value']);


Timer:
create a new timer with a given name:
['customType', 'time:timerName']
pause the specify timer:
['customType', 'time:timerName:pause']
restart the specify timer:
['customType', 'time:timerName:restart']
get the timer elapse time, 
without the pause time, 
if this timer doesn't restart,
then will return the time difference between it pause:
['customType', 'time:timerName:elapse']

waq.push([
	'config', 
	'command', 
	'value'
]);
waq.push([
	'groupDefine',
	//'groupName',
	'groupMember1',
	'groupMember2',
	'groupMember3'
	...
]);
waq.push([
	'group',
	'groupMember',
	'value'
]);
waq.push([
	'type',
	// create a timer with name 'timer1', then record current time stamp to it, then return this time stamp.
	// if timer1 is existed, return the timer's begin time stamp
	'time:timer1',
	// restart the timer, replace it's time stamp with current time stamp
	'time:timer1:reset',
	
	'time:timer1:elapse',
	
	'time:timer1:stop',
	// get current time stamp
	'time:now',
])